/**
 * Gate Pose scoring — same completion (40 %) + form (60 %) split as the other
 * hold-based poses (re-exported from Triangle Pose).
 */
export { getHoldCompletionScore, getFinalMqs } from '@/modules/triangle-pose/scoring';
